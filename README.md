The code can be inserted into google colaboration
After copying the code into google colab, user has to manualy do some commands
!pip install stable_baselines3
Additionally user has to add cleaned_output.csv for data to be processed for Machine Leanring
Then press run and adjust variables such as elacticity, steps, etc,.
